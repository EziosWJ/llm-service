# 本地化智能材料写作平台 — AI 服务层

Python AI 服务，供 Java 后端通过 HTTP 调用，提供文档解析、向量检索和大模型生成能力。Python 服务无状态，不直接访问业务数据库，所有业务上下文由 Java 通过请求传入。

## Language

**材料**:
宣传文档的原始素材，由用户上传，由 Java 后端管理存储和元数据。
_Avoid_: 资料、文档（在业务语境中）

**片段**:
材料经切分后生成的文本块，带有元数据，用于向量检索。
_Avoid_: 分片、chunk（在对外接口和业务讨论中）

**来源片段**:
一次检索命中并返回给写作任务或问答结果用于溯源展示的片段，包含正文、材料 ID、片段序号和相关度。
_Avoid_: source、hit、检索结果（在业务语境中）

**写作任务**:
用户发起的一次内容生成请求，包含写作主题和期望的输出类型（提纲、初稿、润色稿、标题等）。
_Avoid_: 生成任务、请求

**问答**:
用户基于材料提出问题并请求大模型给出回答的一次交互。
_Avoid_: ask、QA、提问接口（在业务语境中）

**Prompt 模板**:
用 Jinja2 编写的模板，根据写作任务和检索到的片段组装最终发送给大模型的提示词。
_Avoid_: 提示词模板、prompt

**材料向量维护**:
Java 后端为保持材料与片段一致而调用 Python 服务写入、替换或删除向量的对接能力。
_Avoid_: 知识库 CRUD、材料 CRUD（指 Python 服务职责时）

## Relationships

- 一个**材料**被解析后产生多个**片段**
- **材料**的主数据、原始文件和权限由 Java 后端管理；Python 服务只负责**材料向量维护**以及基于片段的生成和问答能力
- **材料**在向量库中的唯一边界是 `user_id + material_id`；`material_id` 不承诺全局唯一
- 一个**写作任务**触发一次向量检索 + 一次大模型调用
- 一次**问答**触发一次向量检索 + 一次大模型调用
- 检索返回的**来源片段**被注入 **Prompt 模板** 作为上下文，Prompt 模板默认只消费来源片段的正文
- 一个**来源片段**对应一个被本次检索命中的**片段**，并携带用于展示参考来源和溯源的信息
- **来源片段**的相关度对 Java 后端可见，用于排序、过滤、调试或低置信度提示；不承诺可直接展示给最终用户
- 新写入的**片段**必须关联材料 ID；历史或脏数据缺失材料 ID 时仍可作为**来源片段**返回，但属于溯源降级
- 正常向量检索命中的**来源片段**应带相关度；相关度缺失时仍可返回，但属于排序和调试信号降级
- 正文为空的检索命中不构成**来源片段**，应被忽略
- **来源片段**按检索 Adapter 返回的相关度顺序排列；Python 生成链路不额外重排
- 没有命中**来源片段**时，**写作任务**仍可继续调用大模型
- 没有命中**来源片段**时，**问答**仍可继续调用大模型，但 **Prompt 模板**必须要求不编造并说明材料不足
- **材料**在向量库中全局共享，通过 metadata 字段标记所属用户，检索范围由 Java 后端通过请求参数控制
- **片段**按自然段落切分，超长段落按固定长度二次切分；每个片段携带材料 ID、片段序号、章节标题等元数据
- Java 调用 Python 服务为同步阻塞模式，Python 调用本地大模型后直接返回结果
- 四种生成类型（提纲、初稿、润色稿、标题）共用一个统一端点，通过 `type` 字段区分，内部选择不同 Prompt 模板
- 文档处理（解析 → 切分 → embedding → 写入向量库）为单一端点，Python 内部串联完成
- Java 通过 multipart 文件上传将原始材料文件传给 Python，Python 负责解析
- 生成请求统一模型，润色类型时 `content` 字段必填，其他类型可选忽略
- 本地大模型和 embedding 模型的地址/名称均通过环境变量配置（`LLM_BASE_URL`、`LLM_MODEL`、`EMBEDDING_MODEL` 等）
- 材料删除时 Java 调用 Python 端点按 `user_id + material_id` 清理向量；材料处理接口采用覆盖语义，同一材料重新处理时会先清理旧片段再写入新片段
- Qdrant 集合由 Python 服务启动时自动创建；集合名称包含 embedding 模型标识，换模型时用新集合名实现数据隔离
- 生成和问答请求必须携带 `user_id`；未指定 `material_ids` 时按用户检索，指定非空 `material_ids` 时按 `user_id + material_ids` 精确检索，空列表属于参数错误
- 生成响应和问答响应包含引用的**来源片段**（`sources`），用于展示参考来源和溯源
- 代码分层：api/（端点）→ services/（业务逻辑）→ infrastructure/（外部依赖），models/ 和 templates/ 独立
- 统一错误响应格式，HTTP 状态码区分参数错误(400)、业务校验(422)、上游失败(502)、内部错误(500)
- 健康检查端点（`GET /health`），检查 Python 服务本身、Qdrant 连接、LLM 连接的可用性

## Flagged ambiguities

- "文档"在不同语境中可指原始文件（材料）或最终生成的内容——需根据上下文区分，对外统一用"材料"指代原始素材。
- **来源片段**是否包含章节标题尚未确定；当前代码会记录 `section_title`，但语义不稳定，暂不作为领域承诺。
- 缺失材料 ID 的**来源片段**表示溯源降级，不应导致整个写作任务或问答失败。
- 缺失相关度的**来源片段**表示排序和调试信号降级，不应导致整个写作任务或问答失败。
- TODO: 当前材料处理采用先删后写的覆盖语义，处理失败时可能出现材料暂无可检索片段的空窗状态。Java 后端应将材料标记为处理失败；未来如需保留旧片段直到新片段写入成功，应引入版本化或批次切换机制。
- TODO: 后续明确 Python 侧最大文件大小或最大解析文本长度，当前先由 Java 上传链路控制。
